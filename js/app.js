var app = angular.module('friendsList', []);

/*
app.service("friendService", function() {
	this.foo = "bar";
});
*/

// this service and factory do the exact same thing, but how you have to type them is different

/*
app.factory("friendFacService", function() {
	return { foo: "bar" };
});
*/